{
  "name": "cruize-testing-vaults",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ cruize-subgraph-starter",
    "create-local": "graph create --node http://localhost:8020/ cruize-subgraph-starter",
    "remove-local": "graph remove --node http://localhost:8020/ cruize-subgraph-starter",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 cruize-subgraph-starter",
    "test": "graph test -d"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.34.0",
    "@graphprotocol/graph-ts": "0.28.0",
    "matchstick-as": "^0.5.0"
  }
}